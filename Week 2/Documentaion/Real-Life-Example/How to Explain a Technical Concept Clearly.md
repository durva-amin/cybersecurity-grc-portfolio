Instead of writing:

> "DNS performs recursive resolution through hierarchical name servers."

For a beginner-focused document, you could write:

> **DNS converts domain names into IP addresses so devices can locate websites and other network services.**

The second explanation is easier to understand while remaining technically useful.