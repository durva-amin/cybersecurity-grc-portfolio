Imagine a server has IP address:

**`192.168.1.100`**

It is running:

- Web service → `192.168.1.100:80`
- HTTPS service → `192.168.1.100:443`
- SSH service → `192.168.1.100:22`

The **IP address identifies the server**, while the **port identifies the service endpoint**.