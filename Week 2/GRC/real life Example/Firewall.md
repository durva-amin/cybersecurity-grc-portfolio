An office firewall is configured to block incoming connections from suspicious sources while allowing employees to access approved websites.

The firewall checks the traffic and applies its rules before allowing or blocking it.