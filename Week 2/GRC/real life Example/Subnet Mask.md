An office uses:

**IP:** `192.168.1.25`  
**Subnet Mask:** `255.255.255.0`

The network is:

**`192.168.1.0/24`**

The device is:

**`192.168.1.25`**

The subnet mask helps determine that the first three octets identify the network and the last octet identifies the host.