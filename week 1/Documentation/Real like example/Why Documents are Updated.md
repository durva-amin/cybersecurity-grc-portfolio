A company's **Password Policy** required a minimum password length of **8 characters**.

After new security recommendations, the policy is updated to require **12-character passwords** and **Multi-Factor Authentication (MFA)**.

The document is updated to reflect the new security requirements.