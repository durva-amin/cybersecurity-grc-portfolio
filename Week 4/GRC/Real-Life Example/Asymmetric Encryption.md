# Real-Life Example

Secure web communication, such as **HTTPS/TLS**, can use public-key cryptography during authentication/key establishment.

After a secure session is established, efficient **symmetric encryption** is typically used to protect the actual session data.

So:

> **Asymmetric → Helps establish/authenticate securely**  
> **Symmetric → Efficiently encrypts bulk session data**