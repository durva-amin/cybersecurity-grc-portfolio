Suppose an application needs Windows to remember a particular configuration.

The application may store that configuration in the Registry.

Similarly, during a security investigation, an analyst may examine Registry entries to determine whether a suspicious program configured itself to start automatically.