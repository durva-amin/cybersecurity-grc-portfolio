A company has a project directory that should be managed by the development team.

An administrator can assign the appropriate owner and group:

sudo chown durva:developers project/

Now the ownership is associated with the correct user and group.