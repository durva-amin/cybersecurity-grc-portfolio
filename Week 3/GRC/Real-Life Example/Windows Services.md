Windows Update uses Windows services to perform update-related tasks in the background.

You don't need to manually keep the update process running. The relevant service handles the task in the background.