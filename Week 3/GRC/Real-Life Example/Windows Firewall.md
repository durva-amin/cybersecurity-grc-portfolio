Suppose a program on your computer tries to accept an incoming network connection.

If Windows Firewall has a rule that blocks that type of connection:

**Incoming Connection → Firewall → Blocked ❌**

If the connection is allowed by the firewall rules:

**Incoming Connection → Firewall → Allowed ✅**